/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    allowedDevOrigins: ['http://localhost:3000'], // Or specify like ['http://localhost:3000']
  },
};

module.exports = nextConfig;
